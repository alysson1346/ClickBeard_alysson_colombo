import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  colors: {
    primary: "#222222",
    secondary: "#ffcb05",
    white: "white",
    column: {
      even: "#dbdbdbde",
      odd: "#cfcfcf",
    },
  },
});

export default theme;
